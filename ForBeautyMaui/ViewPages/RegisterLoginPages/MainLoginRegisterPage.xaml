<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"  
    xmlns:renders="clr-namespace:ForBeautyMaui.Renders"
    x:Class="ForBeautyMaui.ViewPages.MainLoginRegisterPage"
    NavigationPage.HasBackButton="False"
    xmlns:language="clr-namespace:ForBeautyMaui.Resources.Languages">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Stroke="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button BackgroundColor="White" Grid.Column="0" FontFamily="times" Text="Login" TextColor="Black"
                            Command="{Binding BtnLoginPage}">
                    </Button>
                    <Button BackgroundColor="White" Text="Register" FontFamily="times" TextColor="Black" Grid.Column="1"
                            Command="{Binding BtnRegisterPage}">
                    </Button>
                    <BoxView HeightRequest="0.5" VerticalOptions="End" Grid.Column="0" Grid.ColumnSpan="2" BackgroundColor="LightGray" IsVisible="True"/>
                    <BoxView x:Name="LoginLine" HeightRequest="0.5" VerticalOptions="End" Grid.Column="0" BackgroundColor="Black" IsVisible="{Binding IsLoginVisible}"/>
                    <BoxView x:Name="RegisterLine" HeightRequest="0.5" VerticalOptions="End" Grid.Column="1" BackgroundColor="Black" IsVisible="{Binding IsRegisterVisible}"/>

                </Grid>
            </Border>


            <StackLayout x:Name="StackLogin" Grid.Row="1" IsVisible="{Binding IsLoginVisible}" >
                <Border Stroke="Transparent" Padding="10">

                    <StackLayout Spacing="15" 
            Padding="0"
            HeightRequest="500">
                        <Label IsVisible="{Binding ErrorMessageLbl}" FontFamily="times"
          TextColor="Red"
          Text="{Binding ErrorMessageLblText}"
          HorizontalOptions="End"
          
          FontSize="Small"/>
                        <StackLayout Spacing="0" Margin="15">
                            <Label Text="رقم الهاتف"  FontFamily="times" FontSize="Medium"
          TextColor="#555555"
          HorizontalTextAlignment="End"
          Margin="0,0,-5,0"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="7*"/>
                                </Grid.ColumnDefinitions>
                                <Image Source="libyan_flag" HeightRequest="25" HorizontalOptions="Start" Grid.Column="0"/>
                                <renders:BorderLessEntry x:Name="phonenEntry" Grid.Column="1" HorizontalOptions="Fill" 
                                FontFamily="times" FontSize="Medium" 
                                Text="{Binding LoginPhoneEnt}"
                                Keyboard="Telephone"
                                Placeholder="092xxxxxxxx" 
                                HeightRequest="30" 
                                Margin="0,10,0,0"/>
                            </Grid>
                            <BoxView HeightRequest="1" HorizontalOptions="Fill"  BackgroundColor="#777777" VerticalOptions="Start"/>
                        </StackLayout>
                        <Label Text="كلمة المرور"  FontFamily="times" FontSize="Medium"
          TextColor="#555555"
          HorizontalTextAlignment="End"
          Margin="0,0,10,0"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="7*"/>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>

                            <renders:BorderLessEntry Grid.Column="0" IsPassword="{Binding IsPasswordBtn}" HorizontalOptions="Fill" Text="{Binding LoginPasswordEnt}"
                                Placeholder="********" HeightRequest="30" Margin="20,10,0,0"/>
                            <Image Grid.Column="1" Source="{Binding ImageshowHide}" HeightRequest="25" Margin="0,0,12,0">
                                <Image.GestureRecognizers >
                                    <TapGestureRecognizer x:Name="TapShowHidePassword" Command="{Binding TapShowHidePasswordTapped}"/>
                                </Image.GestureRecognizers>
                            </Image>
                        </Grid>
                        <BoxView HeightRequest="1" HorizontalOptions="Fill" Margin="15,-15,15,0" BackgroundColor="#777777" />
                        <Grid>
                            <Button x:Name="ButtonLoginClick" BackgroundColor="Black"
             TextColor="White"
                         CornerRadius="5"
             Text="{Binding BtnLoginName}" FontFamily="times" FontSize="Medium"
             Margin="12,20,12,12"
             Command="{Binding LoginBtn}"/>
                            <!--lottie:AnimationView x:Name="lottieLoading" Animation="LoadingLabel.json" AutoPlay="False"
                        WidthRequest="150"
                        HeightRequest="50"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"
                        IsVisible="{Binding AnimationLoginBtn}"
                        RepeatMode="Infinite"
                        Margin="0,10,0,0"
                        /-->
                            <!--
             </Grid>
             <Label Text="هل نسيت كلمة المرور ؟"  FontFamily="times" 
          FontSize="Small"
     TextColor="Gray"
     VerticalOptions="Start"
     HorizontalOptions="Center"
          Margin="0,20,0,0"/>
             <Button Text="إعادة كلمة المرور"  FontFamily="times" 
           FontSize="Medium"
           HorizontalOptions="Center"/>

         </StackLayout>
     </Border -->
                        </Grid>
                    </StackLayout>
                </Border>


            </StackLayout>

            <StackLayout x:Name="StackRegister"  Grid.Row="1" Spacing="15" BackgroundColor="White"
                         IsVisible="{Binding IsRegisterVisible}"
               Margin="20">
                <Label IsVisible="{Binding ErrorLblVisible}"
             TextColor="Red"
             HorizontalOptions="End"
             x:Name="ErrorMessageLbl1"
             FontSize="Medium"
             Text="{Binding ErrorMessageText}"
             FontFamily="times"/>
                <StackLayout Spacing="0" Margin="15">
                    <Label Text="رقم الهاتف" FontFamily="times" FontSize="Medium"
             TextColor="#555555"
             HorizontalTextAlignment="End"
             Margin="0,0,5,0"/>
                    <StackLayout Orientation="Horizontal" Spacing="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="7*"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="libyan_flag" HeightRequest="25" HorizontalOptions="Start" Grid.Column="0"/>
                            <renders:BorderLessEntry  Grid.Column="1" HorizontalOptions="Fill" 
        FontFamily="times" FontSize="Medium" 
        Text="{Binding LoginPhoneEnt}"
        Keyboard="Telephone"
        Placeholder="092xxxxxxxx" 
        HeightRequest="30" 
        Margin="0,10,0,0"/>
                        </Grid>
                        <BoxView HeightRequest="50" WidthRequest="500"  BackgroundColor="Black" VerticalOptions="Start"/>
                    </StackLayout>
                    <StackLayout Padding="0"
                   Margin="5,10,5,0">
                        <StackLayout Orientation="Horizontal" 
                       Spacing="5">
                            <Label Text="إقـرأ الـمـزيـد>>"
                     TextColor="Blue"
                     FontSize="Small"
                     VerticalOptions="Center"
                     HorizontalOptions="EndAndExpand">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding CommandReadMore}" />
                                </Label.GestureRecognizers>
                            </Label>
                            <Label Text=" شروط وأحكام For Beauty" FontFamily="times" FontSize="Medium"
                     />

                        </StackLayout>
                        <StackLayout Orientation="Horizontal"
                      
                       Spacing="5">
                            <Label Text="إقـرأ الـمـزيـد>>"
                     TextColor="Blue"
                     FontSize="Small"
                     VerticalOptions="Center"
                     HorizontalOptions="EndAndExpand">
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding CommandReadPolicy}"  />
                                </Label.GestureRecognizers>
                            </Label>
                            <Label Text=" سياسات الخصوصية For Beauty" FontFamily="times" FontSize="Medium"/>

                        </StackLayout>
                        <StackLayout Orientation="Horizontal"
                       Padding="0"
                       x:Name="stackcheck"
                       FlowDirection="RightToLeft">
                            <Frame x:Name="FrameWhiteCheck" HasShadow="False" HeightRequest="15" WidthRequest="15" BorderColor="Black"
                     BackgroundColor="White"
                     CornerRadius="10"
                     Padding="3"
                    HorizontalOptions="Start"
                        Margin="10,0,0,0"
                        VerticalOptions="Center">
                                <Frame x:Name="FrameBlackCheck" CornerRadius="7"
                    BackgroundColor="{Binding CheckBoxBackgroundColor}"
                         HasShadow="False">
                                </Frame>
                            </Frame>
                            <Label Text="أوافق علي أستخدام تطبيق For Beauty" FontFamily="times" FontSize="Medium"
                     Margin="0,0,-5,0"
                      HorizontalOptions="Start"
                 VerticalOptions="Center"/>
                            <StackLayout.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding TapCheckCommand}" />
                            </StackLayout.GestureRecognizers>

                        </StackLayout>
                        <Grid>
                            <Button x:Name="ButtonRegisterClick" BackgroundColor="Black"
                TextColor="White"
                            CornerRadius="5"
                Text="{Binding LoadingButtonText}" FontFamily="times" FontSize="Medium"
                Margin="0,25,0,12"
                Command="{Binding CommandCountinueRegister}"/>
                            <!--lottie:AnimationView /!-->
                        </Grid>
                    </StackLayout>
                    <Label x:Name="Copyright1"
    HorizontalOptions="Center"
    VerticalOptions="EndAndExpand"
    Margin="0,0,0,35"
    TextColor="Gray"
    FontSize="Small"/>
                </StackLayout>


































            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>








       

